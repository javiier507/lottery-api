name: Scrapping LLM
on:
  workflow_dispatch:
  schedule:
    - cron: '0 21 * * *'

jobs:
  scrapping-llm:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    env:
      LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
      LLM_API_MODEL: ${{ vars.LLM_API_MODEL }}
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: '22'
    - name: Run scrapping LLM
      run: npx tsx src/tasks/scrapping-llm.ts